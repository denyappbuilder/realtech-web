# B18 — IndexNow jen změněné URL (19. 9. 2026, dávka 2 — implementace)

**Stav:** implementováno, PR (viz report). Nahrazuje záznam z 15:43
(zaparkováno) — Daniel 19. 9. večer: ANO, první v pořadí.

## Co se mění
- `scripts/indexnow-changed.mjs` (nový): baseline = `head_sha` posledního
  úspěšného běhu `indexnow-after-deploy.yml` na main (jiný než aktuální SHA);
  `git merge-base --is-ancestor` + `git diff --name-only --diff-filter=ACDMR`;
  mapování zdroj → cesta podle existujícího routingu; průnik s ověřenou
  produkční sitemapou.
- `scripts/indexnow-after-deploy.mjs`: `notifyDeployment` má volitelný
  parametr `zmenene: { root, git? }`; CLI ho zapíná. Bez něj stejné chování
  jako dřív (celá sitemapa) — existující testy proto beze změny.
  Výsledek nese `baseline`, `sitemapUrls`, `urls`.
- `.github/workflows/indexnow-after-deploy.yml`: `fetch-depth: 0` (historie
  pro diff). Nic jiného.
- `docs/audit/INDEXNOW.md`: odstavec Scope aktualizován.

## Hranice (BACKLOG B18)
- Jen šablony/komponenty/CSS/skripty/workflows/docs → **žádný ping**, job OK.
- Prázdný výběr → žádný POST.
- Chybí předchozí produkční běh, nebo není předkem → **STOP, 0 POST**.
- Smazaný článek: detail už není v sitemapě → nepošle se; výpisy ano.
- Žádný nový stav, secret, služba.

## Změny testů
Žádný existující assert nezměněn — nový režim je opt-in parametrem, staré
testy volají funkci bez něj a hlídají dál původní řetězec důvěry.
Nový `scripts/test-indexnow-changed.mjs` (12 testů): mapování, kategorie
z reálného článku, průnik se sitemapou, baseline z API, git ancestor/diff,
nový článek (4 URL), jen šablony (0 POST), prázdný diff, bez baseline (STOP),
baseline mimo historii (STOP), smazaný článek, CLI + workflow fetch-depth.

## Ověření
- Cílené indexnow testy 80/80, `astro check` 0 errors, celá sada v PR.
- Po merge: první ostrý běh = tento PR (skripty/workflow/docs) → očekáváno
  `urls: 0`, žádný POST. Výsledek v reportu.
